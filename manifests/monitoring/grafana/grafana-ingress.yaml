apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: jambonz-monitoring
spec:
  rules:
    - host: cpaas-grafana.epacific.net
      http:
        paths:
        - backend:
            service:
              name: grafana
              port: 
                number: 3000
          path: /
          pathType: Prefix

